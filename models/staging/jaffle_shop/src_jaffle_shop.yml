version: 2

sources:
  - name: jaffle_shop
    schema: default
    tables:
      - name: jaffle_shop_customers
      - name: jaffle_shop_orders_csv
        loaded_at_field: _etl_loaded_at
        freshness:
          warn_after: { count: 12, period: hour }
          error_after:
